module.exports = {name: "한국디스코드봇순위", aliases: [], category: "유용한 기능", run: async (client, message, args) => await require("node-fetch")('https://api.koreanbots.cf/bots/get').then(m => m.json()).then(res => message.channel.send(new(require("discord.js")).MessageEmbed().setAuthor('KOREANBOTS', client.guilds.cache.get("653083797763522580").iconURL()).setColor("DARK_BLUE").setDescription(res.data.map((a, i) => `\`${i+1}위\` - **${a.name}#${a.tag}** (${a.id}) :\n${a.servers}서버, ❤${a.votes}, 상태: ${require("../../src/tools/emoiji").user_status[a.status]} ${status[a.status]}\nㄴ 간단한 설명: \`${a.intro}\``).join('\n'))))}; const status = {online: "온라인", idle: "자리 비움", dnd: "다른 용무 중", offline: "오프라인", streaming: "방송 중"}